---
description: 任务拆分入口(保留 slash 入口,内部路由到模块化内核)
stage: tasking
outputs: Task List
---

# 工作流 3:任务拆分

## 必须加载 (Mandatory Core Rules)
- `core/workflow-engine.md`
- `core/stage-gates.md`
- `core/evidence-engine.md`
- `core/anti-hallucination.md`
- `core/codegraph-engine.md`
- `core/challenge-engine.md`
- `core/memory-engine.md`
- `core/simplification-engine.md`
- `references/glossary.md`
- `stages/3-tasking.md`
- `templates/task-list.md`
- `.workflow/implementation-plan.md`
- `C:\knowledge\INDEX.md`
- 当前项目 `docs/architecture/INDEX.md`(若存在)
- **MCP Tool: sequential-thinking**(强制,需贴出回包字段)
- **CLI: codegraph**(强制,初始化层不可豁免)

## 核心规程 (Core Rule)
本工作流运行 `Tasking` 阶段。目标是:将 Implementation Plan 拆分为可独立执行、可独立验证、顺序清晰的 `Task List`。

## 共享知识库公约 (Shared Knowledge Location)
- 7 个工作流共用一套单层跨项目共享知识库:`C:\knowledge`。
- `C:\knowledge` 只保存跨项目长期知识(协作规则、编码偏好、设计偏好、长期经验、流程偏好)。
- 项目结构、模块边界、接口契约、业务规则、项目技术决策必须维护在当前项目目录内(如 `docs/architecture/`),不写入 `C:\knowledge`。
- 禁止在 `C:\knowledge` 中创建 `global/` / `project/` / `global_workflows/` 目录或 core/stages 等冗余副本。

## 必读规程 (Required Reading Procedure — 固定 7 步)
> 本节定义本入口必须触发的物理读取动作。门禁细则与禁止行为见 `core/stage-gates.md` 与 `stages/3-tasking.md`,本入口不重抄。

1. **加载规则**:物理读取本入口列出的"必须加载"中所有 `core/` / `references/` / `stages/` / `templates/` 文件,登记到 Evidence Ledger。
2. **回读上游产物**:物理读取 `.workflow/implementation-plan.md`,登记 `E-id`;若缺失或字段不全,触发 Rollback to /2-方案设计。
3. **验收输入**:在产物 `Upstream Consumption` 章节逐条标注上游 `AC-id` / `M-id` / `Constraints`(`Consumed / Not Applicable / Deferred`),Reference Rate 必须 100%。
4. **探索源码、图谱、知识**:按 `stages/3-tasking.md` 与 `core/codegraph-engine.md` 中本阶段最少动作清单执行 codegraph 查询(每个 `T-id` 至少 1 条来自 `find_callers` / `find_callees` / `trace_flow` 的依赖证据);物理读取 `C:\knowledge\INDEX.md` 与 `docs/architecture/INDEX.md`(若存在),登记 `E-id`。
5. **产出文件**:按 `templates/task-list.md` 创建或更新 `.workflow/task-list.md`,每个任务必须分配 `T-id` 并填写 Goal / Files / Dependencies / Traces(`M-id, AC-id`) / Source Diff Targets / Risks / Validation;最后一个任务必须为 `复核审查与调优 (Review & Tuning)`。
6. **回读自检**:执行 `core/anti-hallucination.md` 中的 Verification Order;核对 Claim Evidence Map 中每条断言的 `Supporting E-ids` 是否真实存在;核对 Upstream Reference Rate=100%。
7. **下一步建议**:全部门禁通过则唯一下一步 `/4-编码实现`;否则只写 `Blockers` + `Rollback Advice`。

## 必须物理读取 (Mandatory Physical Reads)
- `.workflow/implementation-plan.md`
- `C:\knowledge\INDEX.md`
- 当前项目 `docs/architecture/INDEX.md`(若存在;不存在写采用的等价入口)
- 本入口列出的全部核心规则文件与本阶段协议 `stages/3-tasking.md`
- `templates/task-list.md`(产出前必须回读模板字段)

## 阻断违规与门禁细则
本入口的硬门禁、禁止行为、伪证据判定与回退条件统一由以下文件承载,**禁止在本入口重抄**:
- `core/stage-gates.md`:7 步执行序列、Input / Scope / Evidence / Upstream Consumption / CodeGraph / Exit / Rollback Gate。
- `core/anti-hallucination.md`:伪证据黑名单、Claim Evidence Map、工具失败语义、Sequential Thinking 回包字段要求。
- `core/codegraph-engine.md`:初始化层强制规则、本阶段最少动作清单、跳过证据格式、不可用回退路径。
- `core/evidence-engine.md`:证据台账 6 列结构、高置信结论规则。
- `stages/3-tasking.md`:本阶段 Allowed / Disallowed Actions、ID 约定、Required Output、Handoff Checks、Rollback Conditions。

## Required Output Fields (Reference)
> 阶段产物 `.workflow/<artifact>.md` **必须**包含下述字段;字段顺序与定义见 `templates/task-list.md`,语义见 `core/evidence-engine.md` 与 `core/anti-hallucination.md`。

- Upstream Acceptance / Upstream Consumption(覆盖上游所有结构化项,Reference Rate=100%)
- Mandatory Reads
- Evidence Ledger(6 列:ID / Type / Tool or Command / Target / Result / Supports Claim)
- Logic Thinking Evidence(贴出 sequential-thinking 首末条 thoughtNumber / totalThoughts / nextThoughtNeeded)
- CodeGraph Evidence(初始化 + 最少动作 + 跳过证据三选一)
- Knowledge Applied
- 阶段专有内容(详见模板)
- Open Issues / Blockers / Rollback Advice
- Completion Conditions(本阶段闭合的硬条件)
- Knowledge Update Evidence
- Claim Evidence Map(每条完成断言挂真实 E-id;无证据只能写 Blocked / Not verified / Pending)
- Next Step
